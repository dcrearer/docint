[pytest]
# Set the event loop scope for async fixtures to function scope
asyncio_default_fixture_loop_scope = function

# Disable warning summary (warnings from external dependencies are not actionable)
# Use -W to see warnings: pytest -W default test_agent.py
addopts = -p no:warnings
